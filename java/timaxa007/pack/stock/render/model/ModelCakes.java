package timaxa007.pack.stock.render.model;

import net.minecraft.client.renderer.Tessellator;

public class ModelCakes {

	public static void addSliceCake(Tessellator t, double x,  double y,  double z) {
		/*
		t.startDrawingQuads();

		//Up
		t.addVertexWithUV(b_x, b_y, a_z, b_x, a_z);
		t.addVertexWithUV(a_x, b_y, a_z, a_x, a_z);
		t.addVertexWithUV(a_x, b_y, b_z, a_x, b_z);
		t.addVertexWithUV(b_x, b_y, b_z, b_x, b_z);
		
		t.draw();
		*/
	}

}
