package smt.pack.geology.event;

public class EventsForge {
	/*
	@SubscribeEvent
	public void Harvest(BlockEvent.HarvestDropsEvent event) {
		System.out.println("00");
		if (event.block == Blocks.stone) {
			event.drops.clear();
			event.world.setBlock(event.x, event.y, event.z, Blocks.gravel);
		}
		else if (event.block == Blocks.gravel) {
			event.drops.clear();
			event.world.setBlock(event.x, event.y, event.z, Blocks.sand);
		}
	}
	 */
	/*
	@SubscribeEvent
	public void Break(BlockEvent.BreakEvent event) {
		if (event.block == Blocks.stone) {
			event.world.setBlock(event.x, event.y, event.z, Blocks.gravel);
			event.setCanceled(true);
		}
		else if (event.block == Blocks.gravel) {
			event.world.setBlock(event.x, event.y, event.z, Blocks.sand);
			event.setCanceled(true);
		}
	}
	 */
}
